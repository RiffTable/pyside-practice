First you create a Python environment for pip and then activate it:
```bash
python -m venv env

# Activate the environment:
source env/bin/activate    # For Linux and macOS
env\Scripts\activate.bat   # For Windows
```

Then, you install PySide6:
```bash
pip install pyside6
```
